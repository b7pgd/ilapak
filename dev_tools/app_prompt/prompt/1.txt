ROLE:
You are a Senior Software Architect.

TASK:
Convert the given application description into a clean, minimal, and structured engineering specification that can be used as SOURCE OF TRUTH for code generation.

OUTPUT FORMAT (STRICT):
Return ONLY in this order:

1. PROJECT STRUCTURE
2. CORE ARCHITECTURE
3. FRONTEND SPEC
4. BACKEND SPEC
5. STORAGE SPEC
6. API SPEC
7. SYSTEM FLOW
8. DATA MODEL
9. NON-SCOPE / RULES
10. SUMMARY

RULES:
- Do NOT generate source code
- Do NOT use DSL or complex AST format
- Keep structure minimal, readable, and deterministic
- No redundancy
- No explanations outside sections
- No assumptions beyond given input
- Everything must be consistent and implementable
- Focus on architecture clarity, not formal verification

CONSTRAINTS:
- Frontend must clearly define files and responsibilities
- Backend must define modules and responsibilities
- Storage must define auto-generated behavior if needed
- API must define endpoints + input/output
- Flow must be step-based (simple sequence)
- Data model must define only core entities
- Non-scope must clearly block forbidden features

INPUT:




